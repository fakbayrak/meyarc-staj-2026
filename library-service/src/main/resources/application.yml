spring:
  application:
    name: library-service

  datasource:
    url: jdbc:postgresql://localhost:5434/library
    username: library
    password: library

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8090
